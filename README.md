 # Custom RNN
 Simple character level autoregressive deep recurrent model which generates passages of books given a <genre> start special token.
 There are two implementation of the above mentioned net:
 - The custom one where each single RNN Cell is written using pytorch's basic API
 - The high level definition using the popular pytorch API.
